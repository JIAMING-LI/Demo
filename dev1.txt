you state "When you create a merge request or a pull request, Git compares the latest commit of the source branch (in your case, Dev) with the latest commit of the target branch (Master). The diff that is produced shows all changes that would be applied if the source branch were merged into the target branch at that point in time."

but Master branch latest commit show it has fileA , and the merge commit also fileA, shouldn't it show no diff for fileA ？
